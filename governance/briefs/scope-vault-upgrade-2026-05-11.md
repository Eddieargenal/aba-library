# Scope: ABA Wiki Vault Upgrade — Architecture Alignment

## Objective
Build the remaining non-frontmatter features described in `aba-wiki-detailed description.md` v2.3 so the vault fully matches its architecture specification. Frontmatter migration is complete (123/123 pages compliant). This sprint closes the gap between what is documented and what is built.

## Deliverables
1. `scripts/build-index.py` — Python script that parses all frontmatter and emits `indexes/agent-index.md`
2. `indexes/agent-index.md` — LLM-readable index organized by type and tier, generated by the script
3. `.claude/commands/librarian.md` — full operational skill for session open/close protocol
4. `AGENTS.md` (root) — full rewrite to match ABA wiki architecture model
5. `governance/aba/prompts/ingest-new-source.md` — update step 12 to reference `build-index.py` instead of deleted `index.md`
6. `memory/runtime/logs/log.md` — update Format section to reflect `## [YYYY-MM-DD] type | title` standard
7. `governance/schema/changelog.md` — append entry for today's schema-adjacent changes

## In Scope
- Create `scripts/build-index.py` (standard library only: os, re, yaml)
- Run script to generate `indexes/agent-index.md`
- Create `.claude/commands/librarian.md` with full open and close protocol
- Rewrite `AGENTS.md` to describe frontmatter-first query model, ABA operating model, promotion gates, and librarian protocol
- Update `ingest-new-source.md` step 12 reference
- Update `log.md` Format section
- Append to `governance/schema/changelog.md`

## Out of Scope
- Frontmatter field additions (complete — 123/123 pages pass validation)
- Adding content to tool pages missing field instruments (H-3 — domain expert required)
- Cross-linking wikilinks phase (user-controlled, deferred)
- Automated ingest pipeline / n8n integration
- obsidian-local-rest-api plugin setup (infrastructure, not vault content)
- Backfilling old log.md entries to new format (log is append-only)

## Constraints
- All vault files must use `.md` extension
- `log.md` is append-only — never delete or rewrite existing entries
- `AGENTS.md` rewrite must preserve routing to `governance/00_index.md` as governance entry point
- `scripts/build-index.py` must use Python 3 standard library only (os, re, yaml) — no external dependencies
- `build-index.py` must not fail silently on malformed YAML — must log skipped files

## Dependencies and Access
- Vault at `/Users/eddieargenal/Documents/obsidian-vault`
- Architecture doc at `aba-wiki-detailed description.md` (v2.3) — source of truth for all deliverables
- `governance/aba/CLAUDE.md` — existing ABA operating rules (AGENTS.md must not contradict it)
- Python 3 available via system shell
- Claude Code filesystem access — no credentials needed

## Acceptance Criteria
- [ ] `scripts/build-index.py` exists, runs without error, produces valid output
- [ ] `indexes/agent-index.md` exists and lists all 123 wiki pages organized by type
- [ ] `.claude/commands/librarian.md` exists with both open and close protocol fully specified
- [ ] `AGENTS.md` describes frontmatter-first query architecture, ABA wiki structure, promotion gates, and librarian session protocol
- [ ] `AGENTS.md` routes to `governance/00_index.md` for governance questions
- [ ] `ingest-new-source.md` step 12 references `scripts/build-index.py` → `indexes/agent-index.md`
- [ ] `log.md` Format section matches `## [YYYY-MM-DD] type | description` standard
- [ ] `governance/schema/changelog.md` has an entry for 2026-05-11 changes

## Risks and Rollback
- **AGENTS.md rewrite risk**: losing existing routing logic. Mitigation: read current AGENTS.md fully before rewriting; preserve governance routing table.
- **build-index.py YAML edge cases**: malformed frontmatter in 3 pages could crash index generation. Mitigation: wrap per-file parse in try/except, log skipped files.
- **Rollback**: all files are in a git-trackable vault. If any deliverable introduces problems, revert the specific file.

## Assumptions
- Python 3 is available at `/usr/bin/python3` or equivalent on the host system
- The `indexes/` directory already exists (confirmed: contains memory.md, templates.md, tools.md, workflows.md)
- `scripts/` directory does not exist — must be created
- `.claude/commands/` directory exists or can be created
- The librarian skill will be invoked via Claude Code's `/librarian` command

## Open Questions
None — all blocking questions resolved.

## User Approval
Status: pending
